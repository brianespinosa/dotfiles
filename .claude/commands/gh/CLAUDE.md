# commands/gh

Slash commands for GitHub operations. Scripts that support these commands live here alongside them and are private to this namespace. Do not move scripts to a shared location unless intentionally elevating them to global scope.
